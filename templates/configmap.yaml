apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "beabee.fullname" . }}-config
  labels:
    {{- include "beabee.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
